<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="idea.list">
      <field name="name">idea list</field>
      <field name="model">idea.idea</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="idea.action_window">
      <field name="name">idea window</field>
      <field name="res_model">idea.idea</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="idea.action_server">
      <field name="name">idea server</field>
      <field name="model_id" ref="model_idea_idea"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="idea" id="idea.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="idea.menu_1" parent="idea.menu_root"/>
    <menuitem name="Menu 2" id="idea.menu_2" parent="idea.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="idea.menu_1_list" parent="idea.menu_1"
              action="idea.action_window"/>
    <menuitem name="Server to list" id="idea" parent="idea.menu_2"
              action="idea.action_server"/>
    -->
  </data>
</openerp>